# Database

## 🐳 Como subir o Banco de Dados com Docker (Ambiente Local)

Para inicializar o banco de dados do Portal Contábil localmente com todas as credenciais padronizadas, certifique-se de ter o **Docker** instalado e execute o comando abaixo na raiz do projeto:

```bash
docker compose -f compose.dba.yml up -d
```